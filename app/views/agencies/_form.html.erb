<%= form_with(model: agency, local: true) do |form| %>
  <% if agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agency.errors.count, "error") %> prohibited this agency from being saved:</h2>

      <ul>
      <% agency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= form.text_field :name, placeholder: 'enter agency name', class: 'form-control form-input' %>
    </div><br />

    <div class="form-group">
      <%= form.text_field :street, placeholder: 'enter agency street', class: 'form-control form-input' %>
    </div><br />

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
